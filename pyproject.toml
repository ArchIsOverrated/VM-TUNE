[project]
name = "vmtune-api"
version = "0.1.0"
description = "Python API layer for vmtune"
requires-python = ">=3.10"

[build-system]
requires = ["setuptools>=61"]
build-backend = "setuptools.build_meta"

[tool.setuptools.packages.find]
where = ["src"]
include = ["api*"]
